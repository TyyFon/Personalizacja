import OptionSize from './OptionSize/OptionSize'
import styles from './Product.module.scss';
import clsx from 'clsx';
import { useState } from 'react'

const ProductForm = ({
    sizes, 
    basePrice}) => {
        <optionSize sizes = {sizes}/>
};
export default ProductForm